package game

import (
	"io/fs"
)

func loadSettings(fsys fs.FS) {
	// sets := []scene.Setter{
	// 	scene.Settings{},
	// }
	// for _, set := range sets {
	// 	scene.Load(fsys, "settings.toml", set)
	// }

}
